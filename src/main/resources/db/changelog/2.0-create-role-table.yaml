databaseChangeLog:
  - changeSet:
      id: 2
      author: postgres
      changes:
        - sql:
            sql: |
              CREATE TABLE project_company.role (
                  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
                  name VARCHAR(255)
              );
